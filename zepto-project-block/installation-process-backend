# 1. Create your backend project folder and navigate into it
#mkdir zepto-backend
#cd zepto-backend

# 2. Initialize a new Node.js project
npm init -y

# 3. Install backend dependencies
npm install express sequelize mysql2 dotenv cors jsonwebtoken

# 4. (Optional) Install pm2 globally to run your server in production
npm install -g pm2

# 5. Create your .env file manually with these variables (replace with your RDS info):
# PORT=3001
# DB_HOST=your-rds-endpoint.amazonaws.com
# DB_USER=admin
# DB_PASS=password123
# DB_NAME=zeptodb
# JWT_SECRET=supersecret

# 6. Start your backend server using node (for development)
node src/app.js

# OR start your backend server using pm2 (recommended for production)
pm2 start src/app.js --name zepto-backend

# 7. To stop the pm2 process
pm2 stop zepto-backend

# 8. To see pm2 logs
pm2 logs zepto-backend
